require('dotenv').config();const express=require('express');const path=require('path');const helmet=require('helmet');const rateLimit=require('express-rate-limit');const mongoose=require('mongoose');
const app=express();const PORT=Number(process.env.PORT||3000);app.disable('x-powered-by');app.use(helmet({contentSecurityPolicy:false}));app.use(express.json({limit:'50kb'}));app.use(rateLimit({windowMs:60000,max:300,standardHeaders:true,legacyHeaders:false}));app.use(express.static(path.join(__dirname,'public'),{maxAge:process.env.NODE_ENV==='production'?'1d':0}));
app.get('/api/health',(_req,res)=>res.json({ok:true,service:'The Algorithm',version:'1.0.0',database:mongoose.connection.readyState===1?'connected':'unconfigured',timestamp:new Date().toISOString()}));
app.use('/api/auth',require('./routes/auth'));app.use('/api/market',require('./routes/market'));app.use('/api/signals',require('./routes/signals'));app.use('/api/picks',require('./routes/picks'));app.use('/api/performance',require('./routes/performance'));app.use('/api/portfolio',require('./routes/portfolio'));app.use('/api/ai',require('./routes/ai'));app.use('/api/economic-calendar',require('./routes/economic'));app.use('/api/user',require('./routes/user'));app.use('/api/admin',require('./routes/admin'));
app.use('/api',(_req,res)=>res.status(404).json({error:'API route not found'}));app.use((req,res,next)=>{if(req.method==='GET'&&req.accepts('html'))return res.sendFile(path.join(__dirname,'public','index.html'));next();});
async function boot(){if(process.env.MONGODB_URI){await mongoose.connect(process.env.MONGODB_URI,{serverSelectionTimeoutMS:10000});console.log('MongoDB connected')}else console.warn('MONGODB_URI not configured; database-backed routes will be unavailable');return app.listen(PORT,()=>console.log(`The Algorithm listening on ${PORT}`))}
if(require.main===module)boot().catch(e=>{console.error(e.message);process.exit(1)});module.exports={app,boot};
