const test=require('node:test');const assert=require('node:assert/strict');const {qualify}=require('../../services/signalQualifier');test('qualification rejects weak or conflicting setups',()=>{assert.equal(qualify({opportunityScore:80,confluenceScore:70,conflictSeverity:'HIGH',bias:'BULLISH'}).ok,false);assert.equal(qualify({opportunityScore:80,confluenceScore:70,conflictSeverity:'NONE',bias:'BULLISH'}).ok,true)});
