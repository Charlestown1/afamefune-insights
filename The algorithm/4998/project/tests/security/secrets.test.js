const test=require('node:test');const assert=require('node:assert/strict');const fs=require('fs'),path=require('path');test('source contains no obvious secret assignments',()=>{function walk(d){return fs.readdirSync(d,{withFileTypes:true}).flatMap(x=>x.name==='node_modules'||x.name==='.git'?[]:x.isDirectory()?walk(path.join(d,x.name)):[path.join(d,x.name)])}const files=walk(path.join(__dirname,'../..'));for(const f of files){const s=fs.readFileSync(f,'utf8');assert.doesNotMatch(s,/AIza[0-9A-Za-z_-]{20,}|sk_live_[0-9A-Za-z]+|mongodb\+srv:\/\/[^\s]+:[^\s]+@/,'possible committed secret in '+f)}});
