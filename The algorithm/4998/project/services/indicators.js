function sma(a,p){if(a.length<p)return null;return a.slice(-p).reduce((x,y)=>x+y,0)/p} function ema(a,p){if(a.length<p)return null;let k=2/(p+1),e=a.slice(0,p).reduce((x,y)=>x+y,0)/p;for(let i=p;i<a.length;i++)e=a[i]*k+e*(1-k);return e} function rsi(a,p=14){if(a.length<=p)return null;let g=0,l=0;for(let i=a.length-p;i<a.length;i++){let d=a[i]-a[i-1];if(d>0)g+=d;else l-=d} if(l===0)return 100;return 100-(100/(1+g/l))} function atr(c,p=14){if(c.length<=p)return null;let tr=[];for(let i=1;i<c.length;i++)tr.push(Math.max(c[i].high-c[i].low,Math.abs(c[i].high-c[i-1].close),Math.abs(c[i].low-c[i-1].close)));return sma(tr,p)}
function structure(c){if(c.length<5)return 'NEUTRAL';let x=c.slice(-5), highs=x.map(v=>v.high), lows=x.map(v=>v.low); if(highs[4]>highs[2]&&lows[4]>lows[2])return 'BULLISH_HH_HL'; if(highs[4]<highs[2]&&lows[4]<lows[2])return 'BEARISH_LH_LL'; return 'RANGE'}
function breakout(c,n=20){if(c.length<=n)return 'NONE';let last=c.at(-1).close, prior=c.slice(-n-1,-1); if(last>Math.max(...prior.map(x=>x.high)))return 'UP'; if(last<Math.min(...prior.map(x=>x.low)))return 'DOWN'; return 'NONE'}
function score(c){let closes=c.map(x=>x.close), price=closes.at(-1), e20=ema(closes,20),e50=ema(closes,50),e200=ema(closes,200),rr=rsi(closes),m=structure(c),b=breakout(c);let s=50;if(e20&&e50)s+=e20>e50?20:-20;if(e200)s+=price>e200?6:-6;if(rr!==null){if(rr>=55)s+=8;if(rr<=45)s-=8;if(rr>=70)s-=4;if(rr<=30)s+=4}if(m.startsWith('BULL'))s+=12;if(m.startsWith('BEAR'))s-=12;if(b==='UP')s+=12;if(b==='DOWN')s-=12;return Math.max(0,Math.min(100,Math.round(s)));}
module.exports={sma,ema,rsi,atr,structure,breakout,score};
