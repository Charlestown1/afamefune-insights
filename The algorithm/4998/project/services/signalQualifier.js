function qualify({opportunityScore,confluenceScore,conflictSeverity,bias}){if(!['BULLISH','BEARISH'].includes(bias))return {ok:false,reason:'Neutral bias'};if(conflictSeverity==='HIGH')return {ok:false,reason:'High timeframe conflict'};if(opportunityScore<75)return {ok:false,reason:'Opportunity score below threshold'};if(confluenceScore<55)return {ok:false,reason:'Confluence below threshold'};return {ok:true};}module.exports={qualify};
